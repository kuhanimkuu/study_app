<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Flutter's template only adds this to the debug/profile manifests
         (android/app/src/{debug,profile}/AndroidManifest.xml) by default —
         a release build uses only this file, so without it every network
         call (every /api/* request this app makes) is denied at the OS
         level. Real bug found via a real device install, not theoretical:
         manifested as ClientException/SocketException "Operation not
         permitted, errno = 1", easy to misread as a cleartext-traffic or
         adb-reverse problem instead of a missing permission. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required by image_picker's ImageSource.camera (see chat_screen.dart's
         _pickAndSendImage) — without this declared, camera capture is
         unreliable across Android versions/OEMs even though the actual
         capture happens in a separate Camera app process; image_picker's
         own docs call this out explicitly. android:required="false" on the
         camera feature keeps the app installable on camera-less devices
         (e.g. some tablets), since this is one input option among several
         (PDF/audio/web/text), not the app's core function. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- Required by speech_to_text (see core/voice/voice_input_sheet.dart) —
         the 2026-09-14 decision that voice stays client-native, actually
         implemented in the UI for the first time 2026-09-19. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <application
        android:label="study_os_frontend"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- speech_to_text needs this to find the device's speech
             recognition service under Android 11+'s package-visibility
             rules — without it, recognition silently reports
             unavailable even with RECORD_AUDIO granted. -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>
