<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks plain HTTP (cleartext) traffic by default since API 28 —
  this app's server/ is plain HTTP (no TLS, dev/local-first use), reached
  either via the emulator's 10.0.2.2 alias or, on a real device, via
  `adb reverse` tunneling 127.0.0.1 back to the dev machine (see
  core/api/default_base_url.dart and the server settings dialog).

  Scoped to loopback/emulator addresses only, not the whole app — a
  blanket usesCleartextTraffic="true" would silently allow plain HTTP to
  ANY host the user later points the (user-editable) server URL at. If
  you point this app at a real LAN/hosted server over plain HTTP, that
  host needs to be added here too, or the server needs real TLS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
