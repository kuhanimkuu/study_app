# Voice

Status: **not yet implemented** (placeholder from the target project structure).

Speech-to-text / text-to-speech loop - deliberately out of scope for the Python backend (features/voice/ is a stub by design, client-native e.g. Android SpeechRecognizer), so this is where it would live.

Blueprint reference: Section 25 (Voice) of `STUDY_OS_PRODUCTION_BLUEPRINT.md`.
See `STUDY_OS_PROGRESS.md` for the staged build order this fits into.
