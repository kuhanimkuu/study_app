# Shared models

Status: **not yet implemented** (placeholder from the target project structure).

Typed Dart models mirroring server/ai/schemas/ (Block/ModeratorResponse etc.) - currently each feature decodes raw JSON ad hoc.

Blueprint reference: Section 52 (JSON Contracts) of `STUDY_OS_PRODUCTION_BLUEPRINT.md`.
See `STUDY_OS_PROGRESS.md` for the staged build order this fits into.
