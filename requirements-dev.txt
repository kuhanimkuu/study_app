# Test-only dependencies — kept separate from requirements.txt so a
# production install doesn't pull test tooling. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
