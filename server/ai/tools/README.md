# Tool registry

Status: **not yet implemented** (placeholder from the target project structure).

Structured, schema-validated tool definitions the Moderator's plan can reference by name, with authorization checks before execution — the model must not get unrestricted database/filesystem access.

Blueprint reference: Section 50 (Moderator Execution Architecture) of `STUDY_OS_PRODUCTION_BLUEPRINT.md`.
See `STUDY_OS_PROGRESS.md` for the staged build order this fits into.
