# Tutor domain

Status: **not yet implemented** (placeholder from the target project structure).

Will eventually house the live conversational flow currently served by server/routers/ask.py + features/moderator/engine.py — not migrated this pass (routers/ask.py stays as the working implementation; this becomes its target home once the Moderator's plan schema / tool registry / memory architecture are built).

Blueprint reference: Section 6 (AI Moderator), Section 49 (Backend Structure) of `STUDY_OS_PRODUCTION_BLUEPRINT.md`.
See `STUDY_OS_PROGRESS.md` for the staged build order this fits into.
