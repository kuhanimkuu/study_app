# Study OS Progress Log

Companion tracker to `STUDY_OS_PRODUCTION_BLUEPRINT.md`. Updated after every milestone — corrections/decisions that diverge from the blueprint, what's been implemented, and what's next.

---

## Decisions that override the blueprint

- **Hosting/DB (2026-09-14):** HelioHost deployment uses **PostgreSQL**, not SQLite. The app is **not offline-capable** — always requires connectivity. This overrides Section 30 ("Local-First Architecture") and the SQLite-first framing in Section 32 ("Database Strategy") of the blueprint. Local-first/sync-engine architecture is not the plan.

- **`study_os_overview-v2.pdf` is product/UX vision only, not an infra spec (2026-09-14):** This earlier project doc describes a fully local-first, on-device architecture — Python embedded in the Android app via Chaquopy, LLM inference via a native llama.cpp build on-device, SQLite as the source of truth, offline-capable for everything except web research, and a centralized backend as a later optional addition for accounts/sync. User explicitly confirmed the already-built server+Postgres, always-online architecture (above) is the correct one — **the code is the source of truth, not this document.** The doc's product/UX ideas (single-prompt home screen, quick actions, Study Modes, the block-rendering contract) remain useful references for frontend design, since they map closely onto what's already implemented server-side (the `Block` schema already covers text/equation/interactive_graph/diagram/animation/3d/pdf/source/table); its infrastructure sections (§2, §22, §27, §30, §35 — Chaquopy, on-device llama.cpp, offline-first, SQLite, APK bundling) do not apply and should not inform backend or deployment decisions.

---

## Milestones

### 2026-09-14 — Blueprint review
- Read and confirmed full understanding of `STUDY_OS_PRODUCTION_BLUEPRINT.md` (57 sections).
- Captured the hosting/DB correction above.
- No implementation work started yet.

### 2026-09-14 — Security: purged committed secret (`server/.encryption_key`)
- Found `server/.encryption_key` (real 32-byte key, 44-char base64) tracked in git and pushed to the **public** GitHub repo (`kuhanimkuu/study_app`), added in commit `013be49` on the `David` branch. Missing from `.gitignore` (unlike `server/.jwt_secret` and `server/users.db`, which were already correctly ignored).
- Confirmed it's **dead code** — not referenced by any current server or frontend code. The real mechanism is per-user keys generated at signup (`crypto.py`/`db.py`), stored in the gitignored `users.db`. Unaffected by this cleanup.
- Confirmed the file only existed on the `David` branch (not `main`/`Elly`/`Shem`/`Tom`), limiting blast radius.
- Rewrote `David` branch history with `git filter-branch --index-filter` to strip the file from all commits, deleted it from disk, added `server/.encryption_key` to `.gitignore`, committed (`521d4c5`), and force-pushed the cleaned history to `origin/David` (user confirmed both the rewrite and the force-push beforehand).

---

### 2026-09-14 — Security: fixed upload path-traversal + added size/type limits
- Found `server/routers/ask.py`'s `_save_upload` joined client-controlled `file.filename` and `session_id` directly into a filesystem path — a crafted filename (e.g. `a/../../../../evil.py`) split into real `..` path segments once joined, an arbitrary-file-write vector. No upload size cap and no extension/MIME allowlist either (blueprint Section 37 requires both).
- Fixed: destination filenames are now always `<uuid>.<allowlisted-extension>` — the client's filename is never used in the path, only its (allowlisted) extension. `session_id` is validated against `^[A-Za-z0-9_-]{1,128}$`. Uploads are capped at 25 MB, streamed in chunks with the partial file deleted if the cap is hit. Applied to `/api/ask/image`, `/api/ask/pdf`, `/api/ask/audio`.
- Verified with a standalone script exercising traversal via filename, traversal via session_id, disallowed extensions, oversized uploads, and legitimate uploads — all behaved correctly, nothing escaped `UPLOADS_DIR`.

### 2026-09-14 — Phase 1: real JSON contracts for the Response shape (blueprint Sections 28, 49, 52)
- Added `server/ai/schemas/` (new package: `blocks.py` + `__init__.py`) — deliberately placed to match the blueprint's **target backend structure** (Section 49's `server/ai/schemas/`), not bolted onto the existing flat `server/` layout. This is the first concrete step toward that structure; nothing else moved yet (`domains/`, `engines/`, `core/` etc. are still flat/`features/`-based — intentional, incremental, not a rewrite, per Section 54).
- Defined a Pydantic v2 discriminated-union `Block` type covering every block shape `features/moderator/engine.py` and its engines actually produce today (`text`, `equation`, `interactive_graph`, `clarification`, `error`, `source`, `table`, `static_image`, `pdf`, `diagram`, `animation`, `3d`) plus a `ModeratorResponse` model (`blocks`, `session_id`, `activity`). Reverse-engineered from real call sites, not from `json.md`'s aspirational examples alone — caught one real discrepancy this way: the diagram engine's `elements` are `{id, label, x, y}` objects, not bare strings (`json.md`/the moderator's variable naming alone would have suggested `list[str]`).
- Wired `response_model=ModeratorResponse` into all six `/api/ask/*` endpoints in `routers/ask.py`.
- Verified two ways before trusting it: (1) a standalone script ran the moderator's `run()` across ~17 representative tasks (solve/graph/differentiate/integrate/unit_convert/interactive_2d/static_image(+pdf)/memory_query/numeric_integrate/diagram/animation/interactive_3d/simulation/pdf-no-query/project-no-project/unknown-input-type) and validated every returned block against the schema — all passed; (2) started the real FastAPI server, confirmed the OpenAPI schema correctly references `ModeratorResponse`, and ran a full signup → `/api/ask/text` round trip over real HTTP, confirming FastAPI's actual response-validation layer (not just my own `model_validate` call) accepts real moderator output. Test server and test user cleaned up afterward.
- Blueprint Section 28 lists several block types no engine produces yet (`heading`, `video`, `audio`, `quiz`, `flashcards`, `progress`, `recommendation`, `study_task`, `mistake`, `mastery`, `document`) — deliberately not added to the union yet (would be unused/dead schema); add each when a real engine starts producing it.
- Next Phase 1 items still open: RawInput/ClassifiedContent/EngineResult/ModeratorPlan contracts (Section 51/52), tool registry, memory architecture, personality architecture, internal-first retrieval, Knowledge Space model.

### 2026-09-14 — DONE: Postgres + Identity/Knowledge domains + full target structure scaffold
User asked to fully build the app per the blueprint, starting with the project structure. Given the blueprint is a multi-month effort, agreed approach (via AskUserQuestion): a **staged roadmap, tracked here session to session**, not one unbounded sweep.

**New decision locked in**: server-side **Postgres is the source of truth** for learning data (history, progress, mastery) — not the device. Confirmed via AskUserQuestion when the local-first-vs-server-storage question came up while planning. Consequence: **no sync engine needed at all** (blueprint Section 31 is dropped), client becomes thinner. Client-side encryption stays only for BYOK keys, as today.

**Risk flagged to the user (unverified, not assumed)**: HelioHost is free shared hosting — unknown whether it actually offers PostgreSQL, enough RAM for the local Qwen model, or persistent ASGI hosting. Doesn't block local work; user should verify before the deployment phase.

**What shipped**:
1. Scaffolded the full target directory structure (blueprint Sections 49 backend / 29 Flutter) — additive, ~47 placeholder `README.md`s in not-yet-built domains/features, zero behavior change to existing code.
2. Stood up PostgreSQL locally: created a dedicated `study_os_app` role + `study_os` database (not using the `postgres` superuser for the app), via SQLAlchemy 2.0 async + asyncpg + Alembic. Connection string lives in a gitignored `.env` (`.env.example` documents the shape).
3. Built `server/domains/identity/` (auth/account, same URL paths/shapes as the old `routers/auth.py`+`account.py`, now Postgres-backed via SQLAlchemy) and `server/domains/knowledge/` (formalizes "projects" → Knowledge Spaces per Section 14: adds a `parent_id` column for future nesting the old system never had, and a new `materials` table tracking real per-upload metadata the old system never recorded — material *content*/chunk storage/search stays on the existing working JSON+fastembed mechanism, pgvector migration explicitly deferred).
4. `server/core/security.py` (moved from `server/security.py`) — `get_current_user` is now async and Postgres-backed, still returns a plain dict so `routers/ask.py`'s existing `current_user["id"]`-style access needed zero changes beyond the import path.
5. Retired `server/db.py`, `routers/auth.py`, `routers/account.py`, `routers/projects.py`, `server/security.py` — grepped the whole repo first to confirm nothing else referenced them, then removed via `git rm` (tracked/reversible, not a bare `rm`).
6. Wrote `server/scripts/migrate_sqlite_to_postgres.py` (one-off, preserves original primary keys, resets Postgres sequences afterward) and ran it against the real `server/users.db` — migrated 1 user + 1 knowledge space (the actual dev account, not test data), verified row-for-row in Postgres afterward.
7. Wrote a real integration test suite (`server/tests/`, 9 tests, pytest + pytest-asyncio + httpx, runs against real Postgres) covering signup/login/duplicate-email/wrong-password/me/account-update/auth-required/knowledge-space-CRUD/material-upload. All 9 pass. (Hit and fixed a real pytest-asyncio + asyncpg + Windows footgun: our `engine` is a module-level singleton matching the app's actual lifetime, but pytest-asyncio's default is a fresh event loop per test — fixed via `asyncio_default_fixture_loop_scope = session` / `asyncio_default_test_loop_scope = session` in `pytest.ini`, not by changing the app's engine lifecycle.)
8. Ran a full real-HTTP end-to-end smoke test (live `uvicorn` server, not just ASGI-in-process) covering the entire flow including `/api/ask/text` (confirming the Moderator path still works with the new `security` import) and `/api/ask/project` — all passed. Test server, test account, and all its generated artifacts/uploads cleaned up afterward.

**Deliberately deferred to future sessions** (next items in the staged roadmap, roughly in this order):
- Flutter restructure (`chat`→`tutor/chat`, `projects`→`knowledge/spaces`, `account`→`profile`, `history`→`progress/history`) — real refactor, kept independent of this pass's backend DB migration so each is separately verifiable.
- Google Sign-In (blueprint Section 4 "Required") — separate OAuth/Google-Cloud-project concern.
- Postgres+pgvector migration of material chunk storage/search (currently JSON+fastembed).
- Mastery, spaced repetition (FSRS), assessment engine, misconception engine, planner, personality profiles, structured tiered memory, Moderator plan schema/tool registry, `/api/v1` versioning of existing endpoints, background workers, migrating the 84 existing `features/` engine files into `server/engines/` (blueprint itself says not required immediately).
- Updating `features.md`/`PATHWAY.md`/`json.md`, which this session's research found to be **stale** relative to actual code (e.g. they undercount how much of `visual_explanation`/`ocr`/`rag` is already real).

### 2026-09-14 — DONE: Backend Phase 2 — Learning Core (Concepts, Mastery/FSRS, Assessment, Misconceptions)
User confirmed: finish the backend fully before moving to the frontend. Continuing the staged-slice approach — this slice is blueprint Phase 2 "Learning Core" (Section 54's own priority order, right after the architecture foundation completed last pass).

**What shipped**:
1. `server/domains/learning/` — `Concept` (belongs to a `KnowledgeSpace`) and `Mastery` (per user+concept). Mastery state mirrors `fsrs.Card` fields exactly (`fsrs_state/step/stability/difficulty/due/last_review`) and is updated via the real `fsrs` PyPI package (`open-spaced-repetition`'s reference implementation, published default parameters — not hand-rolled formulas, confirmed its API fit cleanly before committing to it). `mastery`/`retrievability` are computed at read time from that state, never stored as separate columns (avoids staleness drift).
2. `server/domains/assessment/` — `Question` (mcq/true_false/multi_select/numerical/equation/short_answer), `QuestionAttempt`, `Misconception`. Grading (`grading.py`) is deterministic for the first five types (exact/set match, tolerance comparison, sympy symbolic equivalence) and AI-evaluated via the existing `model_router` for `short_answer`, clearly labeled `evaluated_by: "ai"` vs `"deterministic"`. Other 7 blueprint Section 18 types (essay/oral/code/matching/ordering/diagram_labeling/graph_interpretation) return a clear "not yet supported" 400, never a fake grade.
3. Misconception detection: a repeated-wrong-answer heuristic (3 consecutive incorrect on a concept → flag; 2 consecutive correct afterward → resolve) — a structural "struggling here" signal, not semantic classification of *what* the misconception is (that needs real content understanding, deferred).
4. New `/api/v1/...` endpoints (blueprint Section 36) — the first versioned ones in this server; existing unversioned endpoints untouched. `server/core/model_config.py` extracted the BYOK `ModelConfig`/decrypt logic out of `routers/ask.py` (now a second real consumer, in the assessment attempt endpoint).
5. **Found and fixed a real production bug while testing, not just a test artifact**: all the new domains' foreign keys only cascaded at the ORM level (`cascade="all, delete-orphan"` on a relationship), not at the database level. A direct `DELETE FROM users` — exactly what blueprint Section 4's required "Account deletion" feature would eventually do — hit a `ForeignKeyViolationError` instead of cascading. Fixed with `ondelete="CASCADE"` on every domain FK (`knowledge`, `learning`, `assessment` models) plus a migration; also added a naming convention to `Base.metadata` (`server/db/base.py`) since Alembic's autogenerate couldn't reliably name new constraints without one — hand-fixed the generated migration's broken `downgrade()` and verified both `upgrade`/`downgrade` actually run, not just `upgrade`.
6. **Found and fixed a real grading bug in testing**: the short-answer AI grading prompt asked the local "tiny" model for a strict `VERDICT: correct\nFEEDBACK: ...` format; the real model didn't follow it (replied `"Incorrect\n<explanation>"` instead), and the strict parser rejected a perfectly usable answer as "unparseable". Replaced with a parser that finds whichever of "correct"/"incorrect" appears first in the response, verified against the actual model's real output (not just synthetic test strings) before and after the fix.
7. New pytest module `server/tests/test_learning_and_assessment.py` (13 tests, real Postgres, parametrized across all 5 deterministic question types) — all pass, alongside the existing 9 (22 total). Full real-HTTP smoke test covering the AI-graded `short_answer` path specifically (excluded from the fast pytest suite since it needs a live LLM call) — verified end to end against the real local model, including realistic FSRS behavior (mastery/next-review shrinking appropriately after a wrong answer). All test data cleaned up afterward, confirmed zero leftover rows.

**Deliberate scope boundaries** (honest, not faked):
- No question auto-generation yet — authored via the new API only; the Moderator isn't wired to any of this yet (Phase 3, future slice).
- Study Sessions + Planner (blueprint Sections 21-22) explicitly deferred to the *next* slice — they depend on Mastery (built here) but are their own real scope.
- Grading maps correctness to only 2 of FSRS's 4 ratings (correct→Good, incorrect→Again) since no timing/confidence signal is collected this slice.

### 2026-09-14 — DONE: Backend Phase 2 (cont.) — Study Sessions + Planner
Continuing the staged backend build. Shipped `server/domains/planning/` — `Goal` and `StudySession` models, plus `planner.py`'s prioritization/phase-generation logic. New endpoints: `POST/GET/DELETE /api/v1/goals`, `GET /api/v1/plan`, `POST/GET /api/v1/study-sessions`, `GET /api/v1/study-sessions/{id}`, `POST /api/v1/study-sessions/{id}/complete`.

This completes blueprint Phase 2 "Learning Core" in full (Concepts/Mastery/Assessment/Misconceptions from the last slice + Study Sessions/Planner from this one).

**Scope boundaries decided during planning, before writing code**:
- Blueprint Section 22's priority formula includes `prerequisite_importance`, which needs a knowledge graph (Section 15, unbuilt) to compute — **honestly omitted** this slice rather than faked with a placeholder. Priority = blend of review urgency (from Mastery's FSRS `fsrs_due`), weakness (`1 - mastery_score`, reusing `learning/scheduler.py`'s function), and deadline urgency (from a Goal's `target_date`, if any) — "need to learn" (no Mastery row yet) gets a high fixed base score (0.8).
- Blueprint Section 21's 6-phase example includes `worked examples`/`mistakes` phases that need the Moderator to actively teach mid-session — that integration doesn't exist yet (Phase 3, future). This slice's "session" is a real prioritized **plan** (which concepts, what order, how long, why), not yet a Moderator-driven guided experience; 4 phases instead of 6 (retrieval_practice/focus_weak_concepts/practice/reflection).
- `Goal`/`StudySession` FKs got `ondelete="CASCADE"` from the start this time — applying last slice's bug fix proactively rather than retrofitting after the fact. Alembic's naming convention (added last slice) meant this migration's constraints got explicit names automatically, no hand-fixing needed this time.

**Verified**: 6 new pytest tests (real Postgres) — goal CRUD, empty-state (zero concepts) returns a well-formed empty plan rather than erroring, a never-attempted concept scores as "need to learn", an overdue/never-studied concept outranks one just answered correctly, full study-session lifecycle (create → fetch → list → complete → can't double-complete), cross-user ownership enforcement. Combined with the previous two slices: 28 tests total, all passing. Real-HTTP smoke test exercised the full flow including a near-deadline Goal's effect on prioritization (watched it correctly pull every concept's urgency up while still ranking the just-reviewed concept lowest among the four) — behavior matched the intended design, not just "didn't crash". Confirmed zero leftover test data after both the pytest run and the smoke test.

**Backend Phase 2 (blueprint Section 54) is now complete.** Next up per the blueprint's own phase order: Phase 3 "Adaptive Moderator" — wiring student state/learning state/memory/personality into the Moderator's actual teaching behavior (currently `features/moderator/engine.py`'s rule-based routing has no awareness of any of the Postgres-backed domains built these last two slices; `/api/ask/*` and `/api/v1/*` are still two disconnected surfaces). That's a substantial, different kind of slice (touches the Moderator's core architecture, not just a new domain) — worth its own planning pass rather than assuming the same shape as the last three.

### 2026-09-14 — DONE: Backend Phase 3 (start) — Adaptive concept explanations
Blueprint Phase 3 "Adaptive Moderator" is the largest, most open-ended phase (memory, personality, tool registry, adaptive teaching). Shipped a well-bounded first slice rather than attempting all of it: `server/ai/moderator/` (`student_state.py`, `explain.py`, `router.py`) — `POST /api/v1/concepts/{concept_id}/explain` adapts its explanation depth to the student's actual Mastery state (never-attempted/low/medium → introductory/reinforcement; high → advanced; an active Misconception forces reinforcement regardless of mastery).

**Explicitly out of scope, named so the boundary stays visible**: structured tiered memory (Sections 8-9, still unbuilt), personality profiles (Section 7, still unbuilt), a general tool registry/plan schema for arbitrary multi-step plans (Sections 50-51 — this slice's "plan" is a fixed two-step shape specific to this one capability, not a generalized dispatcher built ahead of a second capability to prove it against). The existing rule-based `features/moderator/engine.py`/`/api/ask/*` are untouched — additive, not a rewrite.

Reused `features/moderator/engine.py`'s `_author_text()` prompt-then-fallback pattern (not prompt-then-hard-error, since an explanation can honestly degrade to a templated message using the concept's stored description) and `server/ai/schemas/blocks.py`'s `Block`/`TextBlock` contract — first reuse outside `/api/ask/*`, real validation it wasn't over-specialized to one call site.

**Verified thoroughly, including the part that actually matters**: `decide_depth` unit-tested across every mastery boundary and the misconception-override case (all correct, no LLM needed — it's a pure function). 5 new pytest tests (student-state shape, ownership enforcement — confirmed ownership checks short-circuit *before* any LLM call gets made, keeping the suite fast: 33 tests total across 4 modules now, all passing). Then the part a passing test suite alone can't confirm: a real-HTTP smoke test against the live local model, explaining the *same concept* to the *same student* before vs. after gaining mastery (3 correct attempts) — depth genuinely flipped from `introductory` to `advanced` and the generated text differed, not just the metadata. (Honest note: the local "tiny" model's stylistic difference between depths was modest — a model-capability limit, not a bug; the adaptive mechanism itself — correct depth selection, correct prompt construction, real distinct calls — verified correct.) Zero leftover test data confirmed after.

Next per the blueprint's phase order, if continuing further into Phase 3: structured tiered memory (Sections 8-9) is the natural next piece — everything built so far reads Mastery/Misconception directly rather than through any memory layer, which is honest for what exists today but is exactly the gap Section 8 exists to fill.

### 2026-09-14 — Decisions: voice stays client-native, Google Sign-In deferred, local-dev only
User confirmed three open questions before continuing the "finish the backend" push:
- **Voice stays client-native** (Android `SpeechRecognizer`/`TextToSpeech`) — the original pre-existing decision documented in `features/voice/*/README.md` is correct for this project (latency, cost/resources on free hosting, already Android-targeted). `features/voice/*` is intentionally out of scope for the backend, not a gap to fill — voice becomes Flutter/frontend work later.
- **Google Sign-In deferred** — user doesn't have OAuth credentials yet; still logged as a real gap, not abandoned.
- **Staying local-dev only** — no HelioHost deployment work until the user verifies their plan's actual Postgres/Python/ASGI/RAM specs.

### 2026-09-14 — Security hardening batch: rate limiting, email validation, configurable CORS
Closing three items that have sat in "Open questions/gaps" since early in this session:
- `server/core/rate_limit.py` — simple in-memory sliding-window limiter (10 login attempts/5min, 5 signups/hour, per client IP). Explicitly documented limitation: single-process only, no distributed-attack protection — the proportionate choice at this project's current scale, not hidden as a silent gap. Applied to `/api/auth/login` and `/api/auth/signup`.
- Email validation: `SignupRequest`/`LoginRequest` now use Pydantic's `EmailStr` (rejects malformed addresses) plus a normalization step (`.strip().lower()`) — this also fixed a **real pre-existing bug**: without normalization, "User@Example.com" and "user@example.com" would pass the DB's case-sensitive unique constraint as two different accounts.
- CORS is now configurable via `CORS_ALLOWED_ORIGINS` in `.env` (comma-separated), defaulting to `"*"` so local dev needs zero setup — closes the actual gap (no way to tighten it without a code change), not just relocates the TODO comment that was already there.
- Hit a real test-isolation bug while verifying: all pytest requests share one synthetic client IP under httpx's `ASGITransport`, so the new rate limits (correct production behavior) exhausted after ~5 tests. Fixed with an autouse `reset()` fixture in `conftest.py`, not by weakening the actual limits.
- Added 3 new tests exercising the new behavior itself (malformed email rejected, case-insensitive email normalization + login, rate limit genuinely triggers) — 36 tests total now, all passing.
- **Deliberately skipped**: versioning the pre-existing `/api/auth/*`, `/api/account`, `/api/projects/*`, `/api/ask/*` endpoints under `/api/v1`. Versioning exists to manage incompatible changes over time; nothing about these endpoints is changing, so aliasing them now would be pure churn. The convention is established going forward (all new domains already use `/api/v1`) — that's what mattered, not retrofitting stable endpoints that don't need it.

### 2026-09-14 — Connected old chat's memory_query to real server-side data + fixed a mastery_score bug
Closes the first piece of "the old and new systems are disconnected" gap:
- New `server/ai/moderator/server_events.py` builds `{topic, score, labels, timestamp}` events (the exact shape `features/personalization/query_memory` already expects) from real Postgres Mastery + Misconception data. `routers/ask.py`'s `ask_text` now merges these with whatever `local_events` the client submits before calling the moderator — the existing query-understanding logic (time phrases, struggle filtering, topic matching) is untouched, it just receives richer, real input now.
- Verified with a live smoke test that actually matters: created a concept, got it wrong 3 times (triggering a real Misconception), then asked `/api/ask/text` "what did I struggled with?" with **zero client-submitted local_events** — the real concept name surfaced correctly through the original chat endpoint.
- **Found and fixed a real bug while testing, not a test-assumption error**: `learning/scheduler.py`'s `mastery_score` blended `0.6*retrievability + 0.4*accuracy`. FSRS retrievability sits near 1.0 immediately after *any* review (right or wrong — it measures elapsed time since last review, which is ~0 right after answering), so three consecutive wrong answers scored ~0.6 — misleadingly close to "reviewed". Reweighted to `0.4*retrievability + 0.6*accuracy` (accuracy-dominant); verified directly (3 wrong → 0.4, 3 correct → 1.0) before trusting it. This also improves `planning/planner.py`'s prioritization (which reuses `mastery_score` for its "weakness" component) — a just-missed concept now correctly ranks as urgent instead of being under-prioritized by the same bug.
- Performance tradeoff stated honestly, not hidden: `get_server_side_events` runs on every `/api/ask/text` call, not only actual memory queries (avoiding that would mean duplicating the moderator's private detection regex or changing its stable `run()` signature) — two simple indexed queries per call is cheap enough at this project's real scale to not be worth either.
- 2 new fast pytest tests (no LLM needed) for `get_server_side_events` itself. 38 tests total now, all passing, zero leftover data confirmed.

### 2026-09-14 — Extended assessment grading: matching, ordering, essay, fill_in_blank (10 of 14 blueprint types now supported)
`server/domains/assessment/grading.py` gained three more question types:
- `matching` and `ordering` — deterministic (dict comparison and order-sensitive list comparison respectively), same pattern as the existing types.
- `essay` — AI-evaluated like `short_answer`, against `correct_answer` treated as a model-answer/key-points reference (no rubric infrastructure exists yet, stated honestly in the docstring rather than implying the richer rubric+AI blueprint Section 18 describes).
- Refactored the shared LLM-call-and-verdict-parse logic into `_grade_via_llm()`, used by both `short_answer` and `essay` — avoided duplicating the parsing logic `_parse_verdict()` already handles correctly.
- **Found a real grading-quality issue while verifying against the real model, not just checking it returns 200**: the local "tiny" model initially marked a genuinely correct, thorough essay as "Incorrect" — too strict/checklist-minded for essay-length holistic judgment (a harder task than a short-answer right/wrong call). Fixed by rewriting the prompt to explicitly instruct partial/paraphrased coverage as sufficient for "Correct," reserving "Incorrect" for largely off-topic or wrong answers. Re-verified: the same genuinely-correct essay now grades correctly, and a deliberately off-topic essay still correctly grades incorrect. Honest residual note: the *feedback text* (not the verdict) was still sometimes a bit muddled on the local tiny model — a real small-model capability limit, not a bug; a stronger BYOK backend would likely give cleaner explanations.
- Also added `fill_in_blank` (deterministic, reuses `_grade_choice`'s exact-normalized-match logic) — an initial recount against the blueprint's 14 listed types missed it entirely; caught by recounting carefully rather than assuming the earlier tally was right.
- Remaining ungradable types (4 of 14), deliberately, with reasons stated in `grading.py`'s docstring: `oral` (needs the voice pipeline, which stays client-native — separate concern entirely), `code` (needs a sandboxed execution environment — a real security surface deserving its own dedicated pass), `diagram_labeling`/`graph_interpretation` (no defined spatial/visual answer format yet).
- 3 new fast pytest cases added to the existing parametrized deterministic-grading test (matching, ordering, fill_in_blank) — 41 tests total, all passing, zero leftover data confirmed.

### 2026-09-14 — DONE: Knowledge Graph (Concept relationships) + closed the planner's prerequisite gap
Blueprint Section 15's Knowledge Graph describes multiple node types (Concept, Topic, Formula, Definition, Example, Question, Misconception, Skill) and relationships between any of them. **Scoped down honestly**: shipped concept-to-concept relationships only (`requires`/`depends_on`/`related_to`/`part_of`/`contrasts_with`/`applied_to`/`tested_by`, the 7 types Section 15 lists), reusing the existing `Concept` model — not a fully general typed node/edge graph across every entity type, which would be much larger scope for uncertain near-term value.

**What shipped**:
- `server/domains/learning/models.py` — new `ConceptRelationship` (both FKs `ondelete="CASCADE"` from the start; a unique constraint against duplicate edges; a DB-level `CHECK` constraint rejecting self-edges, not just an application-level check).
- New endpoints: `POST/GET /api/v1/concepts/{id}/relationships`, `DELETE /api/v1/concepts/{id}/relationships/{relationship_id}` — both ends of a relationship must belong to the current user (checked via the existing `get_concept_or_404`, same ownership pattern as everywhere else), closed relationship-type set validated against `RELATIONSHIP_TYPES`.
- `planning/planner.py`'s `prerequisite_importance` — explicitly omitted 2 slices ago for lack of relationship data — is now real: a concept gets boosted if other concepts *already in the same planning pass* `requires`/`depends_on` it and are themselves not yet solidly mastered (scoped to the concepts being planned, not the student's whole graph — enough to reorder one planning pass without an extra broad query).
- **Found and fixed a real formula bug while writing the test that was supposed to prove this worked, not after shipping it**: the first version blended prerequisite importance in as `0.85*base + 0.15*prerequisite_importance` — an *average*, which pulls the score toward whichever value is lower. For a never-attempted concept (base 0.8, already high) with a lower prerequisite_importance (0.33), that blend dragged the score *down* to 0.73 — exactly backwards for something meant to be a priority boost. Fixed with an additive headroom boost (`base + 0.15 * prerequisite_importance * (1 - base)`) that only ever moves the score up, verified directly (0.8 → 0.81, not 0.73) before trusting the test that depends on it.
- 6 new pytest tests (relationship CRUD, invalid-type/self-edge/duplicate rejection, cross-user ownership enforcement, and the prerequisite-boost-changes-ranking behavior itself) — 47 tests total now, all passing, zero leftover data. No separate live-server smoke test this time — this feature is fully deterministic (no LLM path), so the real-HTTP pytest coverage already exercises genuine behavior end to end; the live-smoke-test step has only ever been necessary for verifying actual model output, which doesn't apply here.

### 2026-09-14 — DONE: Structured Memory (explicit + episodic)
Blueprint Sections 8-9's 5-tier memory model. **Two tiers already existed, just not labeled "memory"** — learning memory is `Mastery`/`Misconception`/`ConceptRelationship`, session memory is the moderator's existing `_pending` dict + `StudySession` (correctly ephemeral, not something to persist — not touched).

**What shipped**: `server/ai/memory/` (placed under `ai/`, not `domains/` — matches blueprint Section 49's explicit target structure and reuses the placeholder that was already there, a small correction from what the approved plan said) —
- `Memory` model (one table for both tiers, not two — they share the same shape). `explicit`: user-stated preferences via `POST/GET/DELETE /api/v1/memories`, with `source`/`confidence` forced server-side to `"user_stated"`/`1.0` regardless of what the client sends — verified with a dedicated security test, not assumed.
- `episodic`: system-generated only, at two real trigger points already produced by existing code — misconception resolution (`assessment/router.py`'s `_check_misconception`) and a concept's mastery genuinely *crossing* into "mastered" territory (>=0.85, checked via score-before vs score-after so it fires once, not on every correct attempt afterward — the "don't refire" case was the one most likely to have a bug, tested explicitly and passed first try).
- Wired into `student_state.py`/`explain.py`: a stated preference now actually reaches the prompt `/explain` builds.
- **Verified with more than a passing test suite** — a live smoke test stored the preference "explain everything using pirate/sailing analogies," then asked for the same concept explained twice: without the memory the model used a factory analogy; with it, the model genuinely switched to a sailing-ship analogy. Real behavior change from real data, not just a non-empty response field.
- 7 new pytest tests, 54 total now, all passing, zero leftover data confirmed both after the pytest run and the live smoke test.

**Deliberately deferred**: interaction memory (needs `/api/ask/text` integration, separate concern), automated confidence-upgrading from observed behavior (Section 9's weak-inference-to-strong-behavior pipeline — this slice only wires up user-stated confidence=1.0 and system-derived confidence=1.0 events, not a probabilistic inference layer), personality profiles (next slice).

### 2026-09-14 — DONE: Personality Profiles (last major new Phase 3 piece)
Blueprint Section 7. **Scoped to the blueprint's own 8-dimension worked example** (tone/formality/humor/encouragement/directness/challenge_level/verbosity/teaching_style), not its full 14-dimension aspirational list — real typed columns (unlike `Memory`'s JSONB, personality has a small fixed dimension set per user, which is what columns are for).

**What shipped**: `server/ai/personality/` — get-or-create defaults matching blueprint's own example values (no forced setup), `PATCH /api/v1/personality` for partial updates, `prompt.py` translating the profile into a natural-language style instruction appended to `explain.py`'s prompt *after* the substance instructions (depth/misconception/memory) — the ordering is what keeps personality from being able to override them, per blueprint's own rule that personality must never override accuracy/safety/current learning context.

**Found and fixed a real limitation while verifying against the real model, not just checking for 200 OK**: the "concise" verbosity instruction, left purely as prompt text, barely shortened output (923 → 900 chars, ~2.5%) — the local model is weak at self-terminating early even when explicitly told to. Fixed by tying `verbosity` to `max_tokens` mechanically (concise=70, moderate=220, detailed=400) rather than relying only on the model choosing to comply — re-verified with a clean-state comparison (no leftover profile from a prior run muddying the first attempt, which I initially got wrong and caught before trusting the result): 872 chars (default) vs 290 chars (concise), a real ~3x reduction. Honest residual note: the mechanical cap occasionally trails off before a clean sentence boundary rather than gracefully wrapping up — a known trade-off, not treated as fully solved.

Also verified: setting an extreme personality (playful/max humor) and confirming the `reasoning` field and generated text reflect it — same "prove real behavior changed" discipline as the Memory and short-answer-grading slices.

8 new pytest tests, 62 total now, all passing, zero leftover data confirmed.

**Deliberately deferred**: automated personality adaptation from observed behavior (blueprint wants both explicit + observed; only explicit is built) — same honesty boundary as the Memory slice's deferred confidence-upgrading.

**This completes blueprint Phase 3's major new architectural pieces** (adaptive concept explanations → structured memory → personality). Remaining backend gaps are now either infrastructure (pgvector migration, background workers, observability, CI/CD — lower near-term value without an actual deployment target) or items already deferred pending user input (Google Sign-In, HelioHost deployment) or genuinely separate concerns (interaction memory, the 84 `features/` engines' loading convention, extending adaptivity beyond `/explain` into ordinary `/api/ask/text` chat).

### 2026-09-14 — DONE: extending personality + memory adaptivity into ordinary chat
Closed the biggest gap flagged after Phase 3: `/explain` was adaptive, `/api/ask/text` (the primary chat surface) wasn't. **Scoped to personality + explicit memory only** (both user-scoped, apply to any explanation) — NOT mastery-aware depth or misconception-awareness in chat, which need concept-detection from free text (a separate, harder, explicitly deferred feature).

**What shipped**:
- `server/ai/moderator/style.py` (new) — extracted the personality+memory "adaptive style note" logic that used to live inline in `explain.py`, so `/explain` and ordinary chat share one implementation instead of two that could drift. Also centralizes `verbosity_max_tokens()`, reused by both.
- First time this session touching `features/moderator/engine.py` (~1000 lines, stable, deliberately untouched until now). Kept deliberately minimal and mechanical: threaded two new optional parameters (`personality_style: str | None`, `personality_max_tokens: int | None`) through the existing call chain (`run` → `_route` → `_route_text` → `_route_research_or_clarify`/`_author_text` → `_author_general_reply`), defaulting to `None` everywhere so every existing caller — including the file's own `__main__` demo — is byte-for-byte unaffected.
- The second parameter (`personality_max_tokens`) wasn't in the original plan's exact shape — added it after reconsidering during implementation: passing only a text style instruction would have repeated the same unreliable-conciseness problem already found and fixed for `/explain` (the local model doesn't reliably self-terminate early from text instructions alone). Chose an explicit second parameter over stuffing it into the existing `model_config` dict, which would have caused a `**kwargs` collision at the `model_router.run(max_tokens=..., **model_config)` call sites.
- `server/routers/ask.py`'s `ask_text` now fetches (get-or-create) personality + explicit memories, builds the style note, and passes both new parameters through.

**Verified thoroughly, both affected code paths**, not just one:
1. Ran `features/moderator/engine.py`'s own `__main__` demo after the change — the cheapest, most direct regression check on the file this session was most careful with. Exit code 0, every route identical to before.
2. Full 62-test pytest suite — unchanged, all still passing (this was additive, nothing should have needed updating, and nothing did).
3. Real-HTTP smoke test against the live model, both paths: general conversation went from 1031 chars (default personality) to 337 chars (concise+playful) — a real, large, reliable reduction, same mechanism as `/explain`'s fix. The math-explanation path also visibly picked up the playful tone ("like magic... special toy") compared to its normal dry style, confirming personality reaches that path too, not just general chat.

62 tests passing throughout, zero leftover data confirmed after both the pytest run and the live smoke test.

**Still explicitly out of scope**: concept-detection from free text, mastery-aware depth in ordinary chat, misconception-aware correction in ordinary chat — `/explain` remains the only place those apply.

### 2026-09-14 — DONE: Frontend Phase 1 — Concepts, Mastery, Practice
First Flutter work this session. Scoped to the single most complete backend vertical (Concepts/Mastery/Assessment) rather than attempting the blueprint's full nav restructure or all new features at once — nested inside the existing Projects→workspace flow (a new 4th "Concepts" tab), not a new top-level nav item. Confirmed existing conventions by direct exploration before writing anything (not assumed): no typed Dart models anywhere in the app (`ChatMessage` deliberately keeps blocks as raw JSON) — new work follows that, not a new pattern; single `ApiClient` class with raw-JSON methods; the 4-branch loading/error/empty/list pattern from `projects_list_screen.dart`; no state-management library, no theme-constants file, plain constructor injection throughout.

**What shipped**:
- `ApiClient`: 7 new methods (`listConcepts`, `createConcept`, `getConceptMastery`, `explainConcept`, `listQuestions`, `createQuestion`, `submitAttempt`) against `server/domains/learning` and `server/domains/assessment` — same raw-JSON, `Uri.parse`/`.timeout`/`_decode` pattern as every existing method.
- `lib/features/learning/concepts/presentation/screens/`: `concepts_list_screen.dart` (4-branch list, per-row mastery badge fetched via one `getConceptMastery` call per concept — there's no list-mastery-for-a-space endpoint, only list-mastery-for-a-user, so this is a deliberate small N+1 rather than a backend change this slice didn't need) and `concept_detail_screen.dart` (mastery bar, "Explain this to me" reusing the *existing* chat `BlockView`/`TextBlockView` rather than a new renderer, question list).
- `lib/features/practice/questions/presentation/screens/`: `question_practice_screen.dart` (mcq via `RadioGroup<String>` — matched to the `RadioGroup` API `account_screen.dart` already uses, not the deprecated `RadioListTile.groupValue`; true_false same widget; short_answer a plain `TextField`) and `create_question_screen.dart` (type picker limited to mcq/true_false/short_answer, mcq option list with add/remove, true_false answer via dropdown).
- Corrected two path details from the approved plan during implementation (both explained inline, not silent): screens live at `lib/features/learning/concepts/` + `lib/features/practice/questions/` (the actual pre-scaffolded placeholder folders from an earlier session), not the plan's literal `lib/features/knowledge/`; `project_workspace_screen.dart` got a straightforward 4th `TabBar`/`TabBarView` entry plus one `_buildConceptsTab` method, matching its existing 3 tabs.
- `flutter analyze lib` — 0 issues (the only ones hit mid-build were `RadioListTile.groupValue`/`onChanged` deprecation infos, fixed by switching to `RadioGroup` before finishing, not left as noise).

**Verified against the real local Postgres-backed server and real model**, not just compiled: a standalone Python script drove every new endpoint end-to-end (create concept → mastery=0.0 when never attempted → create mcq question → student-facing `listQuestions` confirmed to NOT leak `correct_answer` → correct attempt → mastery 1.0 → incorrect attempt → mastery 0.7 → real `/explain` call returning real generated text) before touching the UI at all, so any UI-level bug would be isolated to the UI rather than tangled with a backend assumption. Then ran the actual Flutter app (`flutter run -d web-server`) in a real Chrome tab and clicked through the full live loop against that same backend: Concepts tab → list with live mastery badges → detail (description, mastery bar, Explain button rendering a real streamed-in generated explanation through the reused block view, question list) → mcq practice both directions (incorrect: 70%→60%; correct: 60%→70%, mastery bar updating live each time) → Add question → true_false question (type dropdown reshaping the form correctly, answer dropdown, save) → true_false practice (correct, mastery 70%→76%). Cleaned up: killed the dev `uvicorn`/`flutter run` processes and deleted the smoke-test user from Postgres afterward, confirmed cascade delete left zero rows in `knowledge_spaces`/`concepts` behind.

**Testing note, not an app defect**: the browser-automation tool's screenshot capture (`Page.captureScreenshot`) intermittently timed out for 10-40s during this session's live click-through, unrelated to any specific widget — confirmed by (a) the backend request log showing no pending/hung requests during the freezes, (b) zero JS console errors, (c) the exact same interaction sequence completing instantly on a retry in a fresh tab. Environment/tooling flakiness under this VM's resource contention (three heavy processes — local LLM inference, Flutter DDC debug build, Chrome — competing for CPU), not a Dart-side hang.

**Deliberately deferred**: full nav restructure, Study Sessions/Planner/Memory/Personality/Knowledge-Graph UI, general chat adaptivity settings UI. Notifications (Section 40) — going with a simple in-app "what's due" version as its own later slice, not real push notifications (needs Firebase + background-worker infra that doesn't exist). Only 3 of 10 gradable question types get a practice UI this slice (mcq/true_false/short_answer) — the rest is a visible follow-up, not an oversight. No screenshots saved to disk this slice (the live browser click-through covered the same ground more thoroughly).

### 2026-09-14 — DONE: Frontend Phase 2 — Planner (Goals + Study Plan + Study Sessions)
Next staged frontend slice, chosen because the backend Planning domain (Goals/`GET /api/v1/plan`/StudySession lifecycle) has been complete since an earlier backend pass but had zero Flutter representation, and blueprint §29 explicitly names this exact capability ("Home should answer: What should I study right now?"). Plan approved via Plan Mode before coding — see `C:\Users\Administrator\.claude\plans\twinkly-sauteeing-sparkle.md`.

**Also clarified this session**: `study_os_overview-v2.pdf` (a separate, earlier local-first/on-device-Python/Chaquopy/offline architecture doc) was surfaced by mistake — not the intended reference. Confirmed with the user: it does not apply; the already-built server+Postgres architecture and `STUDY_OS_PRODUCTION_BLUEPRINT.md` remain authoritative, and wherever the code and any doc diverge, **the code is correct.**

**One necessary backend addition, not a workaround**: `planner.py`'s `_allocate_phases` gained a `concepts: [{id, name}]` field per phase alongside the existing `concept_ids`, since a plan can span/omit specific Knowledge Spaces and there was no batch concept-lookup endpoint the frontend could otherwise use to resolve ids to names. Additive only — `concept_ids` untouched, `test_planning.py`'s three existing assertions on it still pass unmodified. **Caught a real bug via the new test written to prove the addition worked**: the first version only added `concepts` inside the main phase-generation loop, missing the separately-appended `reflection` phase entirely — `KeyError: 'concepts'` on the very first pytest run, fixed immediately, then verified clean (62/62 backend tests passing).

**What shipped**:
- `ApiClient`: 8 new methods (`listGoals`, `createGoal`, `deleteGoal`, `getPlan`, `createStudySession`, `listStudySessions`, `getStudySession`, `completeStudySession`) — first use of query parameters (`Uri.replace(queryParameters: ...)`) anywhere in this file, established cleanly since there was no prior precedent to match.
- `lib/features/planner/presentation/screens/planner_screen.dart` — new top-level container (`TabBar`/`TabBarView`, same shape as `project_workspace_screen.dart`) with "Plan" and "Goals" tabs.
- `lib/features/planner/schedule/presentation/screens/study_plan_screen.dart` — duration `ChoiceChip`s (30/60/90/120 min), optional project-scope dropdown (reusing `listProjects()`), generates a plan and renders it via a shared `PlanPhasesView` widget, "Start this session" creates a real `StudySession` and pushes to it.
- `lib/features/planner/schedule/presentation/screens/study_session_screen.dart` — `PlanPhasesView` (shared with the plan screen, so phase rendering can't drift between "not yet saved" and "saved" plans) plus one `StudySessionScreen` showing status (active/completed) and a "Mark complete" action.
- `lib/features/planner/goals/presentation/screens/goals_list_screen.dart` — 4-branch list matching `projects_list_screen.dart`; create dialog with title, `showDatePicker` for an optional target date, and an optional project dropdown; delete with confirm dialog. Resolves a Goal's `knowledge_space_id` (an internal numeric id) to a display name by matching against `listProjects()`'s own `id` field, not `slug` — caught during implementation that `slug` was the wrong join key before it ever reached testing.
- `lib/app/app_shell.dart` — 5th `NavigationDestination` ("Planner", calendar icon) alongside Projects/Chat/History/Account, same `IndexedStack`+`NavigationBar` pattern, no restructuring of the existing four.
- `flutter analyze lib` — 0 issues.

**Verified against the real local Postgres-backed server**, backend-first then UI: a standalone Python script drove every new endpoint end-to-end first (goal create with/without target date/project scope, plan generation confirming the new `concepts` field matches `concept_ids` 1:1, full study-session lifecycle including the double-complete → 400 path, goal deletion) — all passed before the UI was touched. Then ran the real Flutter app and clicked through the full live loop: Planner tab → Plan sub-tab → scoped a plan to a real project with real mastery data → generated it → confirmed real concept names rendered (not raw ids) → started a session → session screen showed "Active" with the same phases → marked it complete → status flipped to "Completed", action button correctly disappeared → Goals sub-tab → created a goal with a real date-picker-selected date and a real project scope → list correctly showed "Due 2026-09-25 · Planner Smoke Space" → deleted it → back to empty state. Cleaned up: killed the dev `uvicorn`/`flutter run` processes, deleted the smoke-test user from Postgres, confirmed cascade delete left zero rows in `knowledge_spaces`/`goals` behind.

**Testing note, not an app defect**: this slice's live click-through hit a real environment limitation — synthetic mouse-wheel scroll events (both CDP's native wheel simulation and touch-drag) did not move any `ListView` in this Flutter web debug build, across every screen tested, even after a window resize. Root-caused, not just worked around: dispatching a genuine DOM `WheelEvent` directly at the `flt-glass-pane` element via `javascript_tool` scrolled correctly and immediately, confirming the app's scrolling itself works fine — the gap is specific to how this browser-automation tool's synthetic wheel input reaches Flutter's CanvasKit renderer in this environment, not a regression in this slice's code (every `ListView` here uses the exact same widget already working in `concepts_list_screen.dart`/`projects_list_screen.dart`).

**Deliberately deferred**: `planner/calendar` (a full calendar visualization — the flat phase list proves the loop works without it), editing an existing Goal (create/delete only, matching the backend's own CRUD surface — no `PATCH /goals/{id}` endpoint exists to call), making Planner/Home the landing screen or any further nav restructuring (still the same boundary as the Concepts slice), and a notifications tie-in for reviews/deadlines (blueprint §40, already a separate deferred slice).

### 2026-09-14 — DONE: Frontend Phase 3 — Home, Progress, Personality/Memory/Student-Profile settings
User asked to "finish the frontend fully." Audited every backend router first (not assumed) to separate "real capability with zero frontend" from "would need new backend scope" — see the full audit findings folded into the plan file. Plan approved via Plan Mode — see `C:\Users\Administrator\.claude\plans\twinkly-sauteeing-sparkle.md`.

**Placement decisions**: Home is a new 1st bottom-nav tab (confirmed with user via AskUserQuestion) — 6 total destinations. Personality/Memory/Student-profile settings live off the existing Account screen, not a new tab (also confirmed). Progress (mastery/misconceptions/activity) is folded in as a 3rd tab on the existing Planner screen — my own judgment call, not asked, to avoid a 7th cramped nav destination; named explicitly rather than silently decided.

**Backend additions** (all additive, same "resolve ids to names / close a named gap at the source" discipline as prior slices): `list_my_mastery`/`list_misconceptions` enriched with `concept_name` (+ `knowledge_space_slug` on mastery) via a join; new `GET /api/v1/attempts` (confirmed via audit that no attempt-listing endpoint existed anywhere before this — by question, concept, or account-wide); 5 new nullable student-profile columns on `User` (education_level/course/institution/preferred_language/daily_study_target_minutes) plus a migration (`upgrade`/`downgrade` both verified). **Found and fixed a real pre-existing bug while extending `update_account`**: it only ever committed when `display_name` was in the payload — a request containing only the new profile fields would silently do nothing. Rewrote to check each field independently. Also switched `GET /api/account` and `GET /api/auth/me` (session-restore at app startup) to re-fetch the `User` row and return `user.public()`, matching what `update_account` already did — otherwise the new fields would only ever appear after a separate account-screen fetch. 62 backend tests still pass (extended 3 existing tests with new assertions rather than padding the count with near-duplicates).

**Frontend — what shipped**:
- `ApiClient`: 8 new methods (`getMastery`, `getMisconceptions`, `getAttempts`, `listMemories`, `createMemory`, `deleteMemory`, `getPersonality`, `updatePersonality`) plus `updateAccount` extended with the 5 profile fields.
- `lib/features/home/presentation/screens/home_screen.dart` — new landing tab. Auto-loads a default 60-min/all-projects plan (reusing `PlanPhasesView`, not re-rendered) plus soonest-first upcoming goals; an "Adjust" link jumps to the full Planner tab via a callback threaded from `AppShell`.
- `lib/features/progress/presentation/screens/progress_screen.dart` — new 3rd tab on `PlannerScreen`. One consolidated screen (not split into dashboard/history/insights sub-screens, avoiding tab-inside-tab): mastery sorted weakest-first with progress bars, an unresolved-misconceptions "Needs attention" section, and a merged reverse-chronological activity feed (study sessions + attempts, client-side sorted by timestamp, session cards tap through to the existing `StudySessionScreen`).
- `lib/features/profile/presentation/screens/personality_settings_screen.dart` — all 8 personality dimensions (4 dropdowns matched exactly to the backend's closed value sets, 4 sliders 0.0–1.0), reachable from a new "Personalization" section on Account.
- `lib/features/profile/presentation/screens/memory_screen.dart` — explicit-memory CRUD (create/list/delete, matching the backend's own surface), same 4-branch list pattern as every other list screen.
- `account_screen.dart` — new inline "Student profile" section (5 fields, save independent of display name) and two new navigation entries into the screens above.
- `app_shell.dart` — Home inserted at index 0 (6 destinations total: Home/Projects/Chat/Planner/History/Account).
- `flutter analyze lib` — 0 issues (one `unnecessary_string_interpolations` info hit mid-build, fixed before finishing).

**Verified against the real local Postgres-backed server**, backend-first then UI, same discipline as every prior slice: a standalone Python script drove every new/changed endpoint end-to-end first (profile fields save independently of display_name and survive a re-fetch through both `/api/account` and `/api/auth/me`; mastery/misconceptions/attempts all carry real `concept_name`; personality PATCH persists; memory create/list/delete round-trips) — all passed before the UI was touched. Then ran the real Flutter app and clicked through the full live loop: Home showed the real seeded concept and goal on first load → "Adjust" correctly jumped to the Planner tab → Planner's new Progress tab showed real 40% mastery, the real misconception card, and all 3 real wrong-attempt activity rows with correct prompts/timestamps → Account's Student Profile section was pre-filled with the real saved values → Personality screen showed the real previously-saved values (tone=playful, humor=0.90), changed the tone and saved successfully → Memory screen created and then deleted a real fact → Projects tab sanity-checked working correctly after the nav-index shift. Cleaned up: killed the dev `uvicorn`/`flutter run` processes, deleted the smoke-test user from Postgres, confirmed cascade delete left zero rows in `knowledge_spaces`/`goals`/`memories` behind.

**Explicitly deferred, named** (not silently skipped): Flashcards and Notes (no backend model exists for either — real new scope); a standalone search screen (search only exists today inside `/api/ask/project`'s query flow); Voice (client-native, separate effort, already decided); a mandatory onboarding *flow* (the underlying data is now editable via Account instead — a gated first-run wizard is a separate product decision); the `chat`→`tutor/chat`/`projects`→`knowledge/spaces` cosmetic folder renames; a sequential multi-question quiz flow (current practice UI is one question at a time); dedicated `assessments/exams`/`results` screens (folded into Progress's activity feed instead — no distinct "exam" resource exists server-side); `progress/insights` (trend analysis, no screen this pass); and the remaining 7 of 10 gradable question types still lacking practice-UI input widgets (numerical/equation/matching/ordering/essay/fill_in_blank/multi_select).

## Open questions / gaps to resolve

- Blueprint's testing section (47) has no dedicated performance/load-test category — worth adding given always-online (no offline fallback) makes request latency and Postgres query performance on HelioHost more load-bearing than the doc assumes.
- The existing rule-based `features/moderator/engine.py` (~1000 lines) is a mature, well-tested v1 that already covers most of json.md's shapes informally — the blueprint's Phase 1 items (versioned JSON contracts, tool registry, structured Pydantic plan schema) should build on/formalize this rather than replace it outright, per the blueprint's own "not a rewrite" migration principle.
- **The old and new systems are still mostly disconnected** (partially closed 2026-09-14 — `memory_query` now uses real server-side data, see above): `/api/ask/*`'s other routes (math, general conversation, PDF/RAG) still have zero awareness of Mastery/Concepts/Sessions — only `memory_query` and the dedicated `/explain` endpoint are connected to real data. Ordinary "explain X to me" chat doesn't adapt to mastery the way `/explain` does.
- 4 of 14 blueprint Section 18 question types still ungradable (oral, code, diagram_labeling, graph_interpretation — see `grading.py`'s docstring for why each).
- Knowledge Graph (Section 15) is concept-to-concept relationships only (2026-09-14) — not the fuller multi-node-type graph (Topic/Formula/Definition/Example/Skill as distinct nodes) the blueprint describes.
- Structured memory (Sections 8-9) is explicit + episodic only (2026-09-14) — interaction memory (recent-topics/recurring-requests in ordinary chat) and automated confidence-upgrading from observed behavior are still deferred.
- Personality profiles (Section 7) are the blueprint's own 8-dimension example only (2026-09-14) — automated adaptation from observed behavior (vs. explicit-only) still deferred, same boundary as memory's confidence-upgrading.
- Personality + explicit memory now reach ordinary `/api/ask/text` chat too (2026-09-14), not just `/explain`. Mastery-aware depth and misconception-awareness are still `/explain`-only — they need a specific Concept, and there's no concept-detection from free text yet (a separate, real, deferred feature).
- RAG still JSON+fastembed, not Postgres+pgvector.
- Google Sign-In — deferred, user doesn't have OAuth credentials yet (2026-09-14).
- Voice — **intentionally out of scope for the backend** (2026-09-14 decision, confirmed with user): stays client-native (Android `SpeechRecognizer`/`TextToSpeech`), not a gap to fill here.
- Background workers, observability/structured logging, CI/CD, notifications, analytics — not built.
- 84 `features/` engine files still on the old importlib-loading convention, not migrated to `server/engines/`.
- Nothing deployed — local dev against local Postgres only; HelioHost viability unverified (user's call, staying local-only for now per 2026-09-14 decision).
- Flashcards (blueprint §27) and student-authored Notes have **no backend model at all** (confirmed by audit, 2026-09-14) — `"flashcards"` today is only a Studio PDF-generation doc type, not reviewable card data. Real new backend scope before either can get a real frontend.
- No standalone semantic-search endpoint — search only happens today inside `POST /api/ask/project`'s query flow (2026-09-14 audit finding).
- Frontend now covers Concepts/Mastery/Practice, Planner (Goals/Plan/Sessions), Progress, Home, and Personality/Memory/Student-profile settings (2026-09-14). Still-open frontend gaps: a sequential multi-question quiz mode, 7 of 10 question types' practice-UI input widgets, the `chat`→`tutor/chat`/`projects`→`knowledge/spaces` cosmetic renames, and any dedicated onboarding *flow* (the underlying data is reachable via Account today).
