# Table extraction — engine.
#
# What this file is supposed to do:
#   - Detect and extract a table from an image or PDF.
#   - Return the table as structured data (headers + rows).
#
# Input:  image / pdf
# Output: {"table": {"headers": [...], "rows": [...]}}
# Status: ⏳ later phase
#
# The Python structure + JSON contract live in skeleton.py.
# Implement the real logic here.
